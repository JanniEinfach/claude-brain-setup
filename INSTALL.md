# Install

See `README.md` for full installation instructions, including both the quick install and the interactive guided setup.

Quick version:

```bash
chmod +x scripts/install.sh
./scripts/install.sh

# Or for a personalized CLAUDE.md:
chmod +x scripts/setup.sh
./scripts/setup.sh
```
